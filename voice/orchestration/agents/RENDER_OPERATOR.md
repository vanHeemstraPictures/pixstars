# Render Operator Agent Brief

You run the local render pipeline.

## Rules
- use the repository scripts first
- write outputs to `voice/output/candidates/`
- keep logs in `voice/orchestration/state/`
- never publish directly to final assets without the Evaluator and Publisher stages
