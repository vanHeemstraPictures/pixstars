# Ardour State Hooks

This folder is for any scripts or notes that connect Ardour cue playback to lamp-state changes.

Examples:
- pre-roll hook writes `thinking`
- playback-start hook writes `speaking`
- playback-end hook writes `idle`

Keep the logic simple and observable first.
